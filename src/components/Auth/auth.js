import React from 'react';

// Header Components
// signin signup authentication as a Modal/ popup with Google Auth button
function auth() {
  return <div>Signin / Signup / Google Auth button</div>;
}

export default auth;
